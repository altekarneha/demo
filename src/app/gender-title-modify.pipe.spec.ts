import { GenderTitleModifyPipe } from './gender-title-modify.pipe';

describe('GenderTitleModifyPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderTitleModifyPipe();
    expect(pipe).toBeTruthy();
  });
});
